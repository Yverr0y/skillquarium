interface:
  display_name: "Traffic Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $traffic-engineer for this task."

policy:
  allow_implicit_invocation: false
