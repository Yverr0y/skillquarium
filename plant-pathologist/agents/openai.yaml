interface:
  display_name: "Plant Pathologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $plant-pathologist for this task."

policy:
  allow_implicit_invocation: false
