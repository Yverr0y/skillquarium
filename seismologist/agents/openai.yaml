interface:
  display_name: "Seismologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $seismologist for this task."

policy:
  allow_implicit_invocation: false
