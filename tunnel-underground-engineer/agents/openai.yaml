interface:
  display_name: "Tunnel Underground Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $tunnel-underground-engineer for this task."

policy:
  allow_implicit_invocation: false
