interface:
  display_name: "Combinatorialist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $combinatorialist for this task."

policy:
  allow_implicit_invocation: false
