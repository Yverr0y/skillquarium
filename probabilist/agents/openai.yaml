interface:
  display_name: "Probabilist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $probabilist for this task."

policy:
  allow_implicit_invocation: false
