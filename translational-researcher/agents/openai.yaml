interface:
  display_name: "Translational Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $translational-researcher for this task."

policy:
  allow_implicit_invocation: false
