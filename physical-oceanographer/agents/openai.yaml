interface:
  display_name: "Physical Oceanographer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $physical-oceanographer for this task."

policy:
  allow_implicit_invocation: false
