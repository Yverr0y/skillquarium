interface:
  display_name: "Hepatologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $hepatologist for this task."

policy:
  allow_implicit_invocation: false
