Untitled.*
__pycache__/
graphify-out/
.agents/
skills-lock.json

# gstack is a bundled skill collection cloned by install-skills.sh.
# 250MB+ with its own .git — not commitable. install-skills.sh clones it
# fresh and runs its ./setup to symlink the bundle into each agent.
# Sub-skill wrappers and per-host skill folders are generated only when
# gstack/ is installed; they come and go with the install state.
#
# Patterns cover both the skill folders and the generated `<name>.md` wrapper
# notes. `/gstack-*` already catches `gstack-qa/` and `gstack-qa.md`; the
# underscore-prefixed install artifacts need their own pattern because the
# leading `_` puts them outside it.
/gstack/
/gstack-*
/_gstack*

# Agent install-time skill link roots created inside the vault by some
# `npx skills add` hosts. Not part of the skill source tree.
.adal/
.aider-desk/
.augment/
.autohand/
.bob/
.claude/
.codeartsdoer/
.codebuddy/
.codemaker/
.codestudio/
.commandcode/
.continue/
.cortex/
.crush/
.devin/
.factory/
.forge/
.goose/
.grok/
.hermes/
.iflow/
.inferencesh/
.jazz/
.junie/
.kilocode/
.kimchi/
.kiro/
.kode/
.lingma/
.mcpjam/
.minimax/
.moxby/
.mux/
.neovate/
.ona/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.reasonix/
.roo/
.rovodev/
.tabnine/
.terramind/
.tinycloud/
.trae/
.vibe/
.windsurf/
.zcode/
.zencoder/
/agent/

# Local OpenTUI dependencies and the user's invocation-state snapshot.
/.skill-vault/tui/node_modules/
/.skill-vault/skill-toggle-state.json
