interface:
  display_name: "Soil Ecologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $soil-ecologist for this task."

policy:
  allow_implicit_invocation: false
