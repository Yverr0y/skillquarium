interface:
  display_name: "Academic Paper"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $academic-paper for this task."

policy:
  allow_implicit_invocation: false
