interface:
  display_name: "Computational Linguist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $computational-linguist for this task."

policy:
  allow_implicit_invocation: false
