interface:
  display_name: "Biogeochemist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $biogeochemist for this task."

policy:
  allow_implicit_invocation: false
