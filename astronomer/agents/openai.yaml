interface:
  display_name: "Astronomer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $astronomer for this task."

policy:
  allow_implicit_invocation: false
