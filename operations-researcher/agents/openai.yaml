interface:
  display_name: "Operations Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $operations-researcher for this task."

policy:
  allow_implicit_invocation: false
