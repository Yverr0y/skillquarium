interface:
  display_name: "Separation Processes Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $separation-processes-engineer for this task."

policy:
  allow_implicit_invocation: false
