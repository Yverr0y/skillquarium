interface:
  display_name: "Bioprocess Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $bioprocess-engineer for this task."

policy:
  allow_implicit_invocation: false
