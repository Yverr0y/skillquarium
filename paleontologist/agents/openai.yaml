interface:
  display_name: "Paleontologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $paleontologist for this task."

policy:
  allow_implicit_invocation: false
