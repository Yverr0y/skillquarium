interface:
  display_name: "Clinical Epidemiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $clinical-epidemiologist for this task."

policy:
  allow_implicit_invocation: false
