interface:
  display_name: "Solar Physicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $solar-physicist for this task."

policy:
  allow_implicit_invocation: false
