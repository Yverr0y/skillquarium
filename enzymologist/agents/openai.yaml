interface:
  display_name: "Enzymologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $enzymologist for this task."

policy:
  allow_implicit_invocation: false
