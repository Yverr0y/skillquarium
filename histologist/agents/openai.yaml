interface:
  display_name: "Histologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $histologist for this task."

policy:
  allow_implicit_invocation: false
