interface:
  display_name: "Epigeneticist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $epigeneticist for this task."

policy:
  allow_implicit_invocation: false
