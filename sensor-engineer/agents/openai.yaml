interface:
  display_name: "Sensor Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $sensor-engineer for this task."

policy:
  allow_implicit_invocation: false
