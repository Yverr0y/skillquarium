interface:
  display_name: "Psychophysicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $psychophysicist for this task."

policy:
  allow_implicit_invocation: false
