interface:
  display_name: "Dhdna Profiler"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $dhdna-profiler for this task."

policy:
  allow_implicit_invocation: false
