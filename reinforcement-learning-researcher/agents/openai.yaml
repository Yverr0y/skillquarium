interface:
  display_name: "Reinforcement Learning Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $reinforcement-learning-researcher for this task."

policy:
  allow_implicit_invocation: false
