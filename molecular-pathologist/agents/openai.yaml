interface:
  display_name: "Molecular Pathologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $molecular-pathologist for this task."

policy:
  allow_implicit_invocation: false
