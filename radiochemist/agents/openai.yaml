interface:
  display_name: "Radiochemist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $radiochemist for this task."

policy:
  allow_implicit_invocation: false
