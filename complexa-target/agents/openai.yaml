interface:
  display_name: "Complexa Target"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $complexa-target for this task."

policy:
  allow_implicit_invocation: false
