interface:
  display_name: "Bacteriologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $bacteriologist for this task."

policy:
  allow_implicit_invocation: false
