interface:
  display_name: "Geomorphologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $geomorphologist for this task."

policy:
  allow_implicit_invocation: false
