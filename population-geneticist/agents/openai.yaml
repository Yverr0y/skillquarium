interface:
  display_name: "Population Geneticist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $population-geneticist for this task."

policy:
  allow_implicit_invocation: false
