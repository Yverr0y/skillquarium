interface:
  display_name: "Cryptographer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $cryptographer for this task."

policy:
  allow_implicit_invocation: false
