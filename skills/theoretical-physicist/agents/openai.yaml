interface:
  display_name: "Theoretical Physicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $theoretical-physicist for this task."

policy:
  allow_implicit_invocation: false
