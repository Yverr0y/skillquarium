interface:
  display_name: "Pathologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $pathologist for this task."

policy:
  allow_implicit_invocation: false
