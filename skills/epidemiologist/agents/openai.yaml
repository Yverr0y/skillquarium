interface:
  display_name: "Epidemiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $epidemiologist for this task."

policy:
  allow_implicit_invocation: false
