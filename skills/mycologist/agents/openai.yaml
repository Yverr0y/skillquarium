interface:
  display_name: "Mycologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $mycologist for this task."

policy:
  allow_implicit_invocation: false
