interface:
  display_name: "String Theorist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $string-theorist for this task."

policy:
  allow_implicit_invocation: false
