interface:
  display_name: "Drug Discovery Pipeline"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $drug-discovery-pipeline for this task."

policy:
  allow_implicit_invocation: false
