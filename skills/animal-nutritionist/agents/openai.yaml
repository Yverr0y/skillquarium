interface:
  display_name: "Animal Nutritionist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $animal-nutritionist for this task."

policy:
  allow_implicit_invocation: false
