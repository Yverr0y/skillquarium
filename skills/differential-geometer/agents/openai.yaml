interface:
  display_name: "Differential Geometer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $differential-geometer for this task."

policy:
  allow_implicit_invocation: false
