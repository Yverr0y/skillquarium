interface:
  display_name: "Plant Physiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $plant-physiologist for this task."

policy:
  allow_implicit_invocation: false
