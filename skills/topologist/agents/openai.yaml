interface:
  display_name: "Topologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $topologist for this task."

policy:
  allow_implicit_invocation: false
