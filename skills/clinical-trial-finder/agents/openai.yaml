interface:
  display_name: "Clinical Trial Finder"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $clinical-trial-finder for this task."

policy:
  allow_implicit_invocation: false
