interface:
  display_name: "Ethologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $ethologist for this task."

policy:
  allow_implicit_invocation: false
