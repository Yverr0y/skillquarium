interface:
  display_name: "Herpetologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $herpetologist for this task."

policy:
  allow_implicit_invocation: false
