interface:
  display_name: "Bio Orchestrator"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $bio-orchestrator for this task."

policy:
  allow_implicit_invocation: false
