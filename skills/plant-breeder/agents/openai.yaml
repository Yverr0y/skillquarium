interface:
  display_name: "Plant Breeder"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $plant-breeder for this task."

policy:
  allow_implicit_invocation: false
