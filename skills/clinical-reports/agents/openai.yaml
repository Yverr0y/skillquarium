interface:
  display_name: "Clinical Reports"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $clinical-reports for this task."

policy:
  allow_implicit_invocation: false
