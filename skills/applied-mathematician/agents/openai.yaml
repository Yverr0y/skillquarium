interface:
  display_name: "Applied Mathematician"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $applied-mathematician for this task."

policy:
  allow_implicit_invocation: false
