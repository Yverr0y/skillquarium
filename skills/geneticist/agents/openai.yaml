interface:
  display_name: "Geneticist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $geneticist for this task."

policy:
  allow_implicit_invocation: false
