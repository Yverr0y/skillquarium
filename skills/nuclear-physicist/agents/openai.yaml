interface:
  display_name: "Nuclear Physicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $nuclear-physicist for this task."

policy:
  allow_implicit_invocation: false
