interface:
  display_name: "Biochemist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $biochemist for this task."

policy:
  allow_implicit_invocation: false
