interface:
  display_name: "Photochemist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $photochemist for this task."

policy:
  allow_implicit_invocation: false
