interface:
  display_name: "Cardiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $cardiologist for this task."

policy:
  allow_implicit_invocation: false
