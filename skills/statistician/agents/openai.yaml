interface:
  display_name: "Statistician"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $statistician for this task."

policy:
  allow_implicit_invocation: false
