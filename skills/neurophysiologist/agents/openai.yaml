interface:
  display_name: "Neurophysiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $neurophysiologist for this task."

policy:
  allow_implicit_invocation: false
