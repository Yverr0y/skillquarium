interface:
  display_name: "Computer Architecture Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $computer-architecture-researcher for this task."

policy:
  allow_implicit_invocation: false
