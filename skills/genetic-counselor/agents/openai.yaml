interface:
  display_name: "Genetic Counselor"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $genetic-counselor for this task."

policy:
  allow_implicit_invocation: false
