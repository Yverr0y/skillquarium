interface:
  display_name: "Ecotoxicologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $ecotoxicologist for this task."

policy:
  allow_implicit_invocation: false
