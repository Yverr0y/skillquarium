interface:
  display_name: "Meteorologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $meteorologist for this task."

policy:
  allow_implicit_invocation: false
