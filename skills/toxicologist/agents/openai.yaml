interface:
  display_name: "Toxicologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $toxicologist for this task."

policy:
  allow_implicit_invocation: false
