interface:
  display_name: "Bio Metric System"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $bio-metric-system for this task."

policy:
  allow_implicit_invocation: false
