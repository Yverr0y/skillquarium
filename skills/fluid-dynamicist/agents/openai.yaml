interface:
  display_name: "Fluid Dynamicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $fluid-dynamicist for this task."

policy:
  allow_implicit_invocation: false
