interface:
  display_name: "Mass Spectrometrist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $mass-spectrometrist for this task."

policy:
  allow_implicit_invocation: false
