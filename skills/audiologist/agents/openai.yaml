interface:
  display_name: "Audiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $audiologist for this task."

policy:
  allow_implicit_invocation: false
