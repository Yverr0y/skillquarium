interface:
  display_name: "Bio Dataset Search"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $bio-dataset-search for this task."

policy:
  allow_implicit_invocation: false
