interface:
  display_name: "Agroecologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $agroecologist for this task."

policy:
  allow_implicit_invocation: false
