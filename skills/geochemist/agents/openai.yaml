interface:
  display_name: "Geochemist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $geochemist for this task."

policy:
  allow_implicit_invocation: false
