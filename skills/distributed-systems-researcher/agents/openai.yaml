interface:
  display_name: "Distributed Systems Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $distributed-systems-researcher for this task."

policy:
  allow_implicit_invocation: false
