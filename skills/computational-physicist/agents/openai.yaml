interface:
  display_name: "Computational Physicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $computational-physicist for this task."

policy:
  allow_implicit_invocation: false
