interface:
  display_name: "Human Computer Interaction Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $human-computer-interaction-researcher for this task."

policy:
  allow_implicit_invocation: false
