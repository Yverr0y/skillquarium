interface:
  display_name: "Oceanographer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $oceanographer for this task."

policy:
  allow_implicit_invocation: false
