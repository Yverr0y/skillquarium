interface:
  display_name: "Protistologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $protistologist for this task."

policy:
  allow_implicit_invocation: false
