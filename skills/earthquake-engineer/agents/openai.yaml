interface:
  display_name: "Earthquake Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $earthquake-engineer for this task."

policy:
  allow_implicit_invocation: false
