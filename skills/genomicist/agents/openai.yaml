interface:
  display_name: "Genomicist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $genomicist for this task."

policy:
  allow_implicit_invocation: false
