interface:
  display_name: "Agricultural Entomologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $agricultural-entomologist for this task."

policy:
  allow_implicit_invocation: false
