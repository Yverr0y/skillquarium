interface:
  display_name: "Chronobiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $chronobiologist for this task."

policy:
  allow_implicit_invocation: false
