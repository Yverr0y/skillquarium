interface:
  display_name: "Animal Geneticist Breeder"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $animal-geneticist-breeder for this task."

policy:
  allow_implicit_invocation: false
