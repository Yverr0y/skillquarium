interface:
  display_name: "Parasitologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $parasitologist for this task."

policy:
  allow_implicit_invocation: false
