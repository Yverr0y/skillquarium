interface:
  display_name: "Metallurgist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $metallurgist for this task."

policy:
  allow_implicit_invocation: false
