interface:
  display_name: "Civil Engineer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $civil-engineer for this task."

policy:
  allow_implicit_invocation: false
