interface:
  display_name: "Petrologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $petrologist for this task."

policy:
  allow_implicit_invocation: false
