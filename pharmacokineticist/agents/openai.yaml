interface:
  display_name: "Pharmacokineticist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $pharmacokineticist for this task."

policy:
  allow_implicit_invocation: false
