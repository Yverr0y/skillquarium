interface:
  display_name: "Volcanologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $volcanologist for this task."

policy:
  allow_implicit_invocation: false
