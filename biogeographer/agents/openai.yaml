interface:
  display_name: "Biogeographer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $biogeographer for this task."

policy:
  allow_implicit_invocation: false
