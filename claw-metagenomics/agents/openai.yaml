interface:
  display_name: "Claw Metagenomics"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $claw-metagenomics for this task."

policy:
  allow_implicit_invocation: false
