interface:
  display_name: "Global Health Researcher"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $global-health-researcher for this task."

policy:
  allow_implicit_invocation: false
