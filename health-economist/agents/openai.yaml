interface:
  display_name: "Health Economist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $health-economist for this task."

policy:
  allow_implicit_invocation: false
