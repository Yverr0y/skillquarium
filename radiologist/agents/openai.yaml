interface:
  display_name: "Radiologist"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $radiologist for this task."

policy:
  allow_implicit_invocation: false
